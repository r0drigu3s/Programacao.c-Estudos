#include <stdio.h>

int main(){

    int a = 15, b = 5;

    if( a > 0 && b > 0 ) {
        printf("Os dois números são positivos.\n");
    } else {
        printf("Ao menos um dos valores é negativo.\n");
    }
}